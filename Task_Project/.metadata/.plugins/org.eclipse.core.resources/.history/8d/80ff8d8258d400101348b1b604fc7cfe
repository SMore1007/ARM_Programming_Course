/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 *
 ******************************************************************************
 */

// IRQ_no % 32 = bit position from the register

#include <stdint.h>
#include <stdio.h>

#define OTG_HS_IRQ_NUM 77

#define ISPR3 (*(uint32_t *)0xE000e20C)
#define ISER3 (*(uint32_t *)0xE000E10C)

int main(void)
{
	// 1. Manually pend the pending bit for the USART3 IRQ number in NVIC
	ISPER3 |= (0x01 << (OTG_HS_IRQ_NUM % 32));

	// 2. Enable USART3 IRQ number in NVIC
	ISER3 |= (0x01 << (OTG_HS_IRQ_NUM % 32));

	while(1){

	}
}

void OTG_HS_IRQHandler(void){
	printf("in OTG_HS ISR\n");
}

